-- The live Supabase database already has these defaults and index name.
-- This migration records the non-destructive Prisma schema alignment; it does
-- not alter tables, defaults, indexes, or member data.
SELECT 1;
